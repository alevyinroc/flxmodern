Place self-hosted font files (e.g. WOFF2) in this directory.

Example:
- Inter-Regular.woff2
- Inter-Bold.woff2

The theme's `static/css/style.css` includes an example `@font-face` that points to `/fonts/Inter-Regular.woff2`.
Update the file names or add additional `@font-face` rules as needed.
